{
  "name": "@athelas/face-compare",
  "private": true,
  "scripts": {
    "dev": "source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "install:python": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
  }
}
