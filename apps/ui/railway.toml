[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter athelas-ui build"
watchPatterns = ["apps/ui/**"]

[deploy]
startCommand = "pnpm --filter athelas-ui start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
