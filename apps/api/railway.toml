[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter athelas-api... build"
watchPatterns = ["apps/api/**"]

[deploy]
startCommand = "node apps/api/lib/"
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
