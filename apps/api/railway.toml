[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter medapp-api build"
watchPatterns = ["apps/api/**"]

[deploy]
startCommand = "pnpm --filter medapp-api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
