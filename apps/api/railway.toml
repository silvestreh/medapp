[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter athelas-api... build"
watchPatterns = ["apps/api/**"]

[deploy]
startCommand = "pnpm --filter athelas-api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
