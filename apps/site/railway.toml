[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter athelas-site... build"
watchPatterns = ["apps/site/**"]

[deploy]
startCommand = "pnpm --filter athelas-site start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
