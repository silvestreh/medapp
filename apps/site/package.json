{
  "name": "athelas-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 5180",
    "build": "astro build",
    "preview": "astro preview",
    "clean": "rm -rf dist .astro"
  },
  "dependencies": {
    "@phosphor-icons/core": "^2.1.1",
    "astro": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
