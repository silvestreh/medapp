{
  "name": "medapp",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --filter=\"!client\" dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
